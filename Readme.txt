Clone the repository:

git clone https://github.com/dukaludic/pw-manager.git

Install dependencies:

cd pw-manager 
cd client && npm install
cd ../ && npm install

Run server:
\pw-manager\
node server.js

(don't use nodemon)

Run react:
cd client && npm start

